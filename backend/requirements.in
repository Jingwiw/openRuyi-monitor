# Runtime dependencies only. Test tooling lives in requirements-test.in and in
# pyproject.toml's [project.optional-dependencies] test extra. requirements.lock
# is the full pinned closure (runtime + test) tested on Fedora 43 / Python 3.14.
fastapi>=0.115,<1
uvicorn>=0.30,<1
httpx>=0.28,<1
defusedxml==0.7.1
nvchecker==2.22
jq>=1.8,<2
packaging>=24
